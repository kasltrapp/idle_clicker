# Bug Tracker & Fix Log

Running record of confirmed bugs found and fixed during development, plus known open issues. Check here first whenever something behaves like a regression — it may be a previously-fixed issue that resurfaced (e.g. via a package update overwriting a vendored-code fix).

Format per entry: **Date/session · Component · Severity · Status**

---

## Fixed

### Selfie Session and Verified Status had no UI rows at all — permanently blocked the entire Yourgram chain past First Post
**Component:** `Assets/InfluencerRise/Scenes/Main.unity` (`BusinessListPanel/.../Content`) · **Severity: Critical** · **Status: Fixed**

Found while diagnosing a user report of "businesses stop producing income after First Post." Only 3 of the 5 Yourgram `UI_BusinessRow` instances existed in the scene (First Post, Story Feed, Engagement Farming) — Selfie Session and Verified Status had no row at all, despite their `Business` assets existing and being fully functional on the backend (confirmed via direct `TryBuyItems` calls, which succeeded normally). Per `EconomySchema.md`'s lock chain, Story Feed requires "Own 15× Selfie Session AND Level ≥7" — with no UI path to ever buy Selfie Session, that condition could never be satisfied through real play, permanently locking Story Feed (and, cascading, Engagement Farming's "Own 20× Story Feed" requirement) regardless of player level. Commit `7a2a080` ("Complete Yourgram platform: SelfieSession/VerifiedStatus businesses...") only added the business data assets, never their rows — the same "claimed done, wasn't" pattern already documented for commit `c89daa7` elsewhere in this file.

**Fixed** by instantiating `UI_BusinessRow` for both missing businesses and reordering all 5 rows into natural progression order (First Post, Selfie Session, Story Feed, Engagement Farming, Verified Status). Verified end-to-end in real Play Mode via genuine raycast-driven taps on the actual rows (not direct API calls): bought Selfie Session through its own row to satisfy Story Feed's ownership requirement, granted XP via the legitimate `AddXp` API to cross the level requirement (substituting for real playtime, which wasn't practical to wait out in a verification session), and confirmed Story Feed's row flipped from locked/non-interactable to unlocked/interactable, then completed a real purchase through it.

**Risk:** this is a content-coverage gap, not a code bug — the same class of gap could recur for any future Business/Manager asset. See CLAUDE.md Hard Rule #16 (added as a direct result of this bug): any new Business/Manager asset must get a corresponding UI element in the same batch, or the task must explicitly flag it as deferred with a named follow-up.

### `UI_BusinessRow` icon frame had no circular mask — real art would not have cropped correctly
**Component:** `Assets/InfluencerRise/Prefabs/UI/UI_BusinessRow.prefab` (`IconFrame`) · **Severity: High** · **Status: Fixed**

Found during the row template's final rebuild/lockdown pass, specifically the real-art-drop-in verification step this task required before finalizing. `BusinessDisplayer.image` (the field `Redraw_Metadata` actually swaps `Business.Metadata.icon` onto at runtime) pointed directly at `IconFrame`'s own `Image` component — a plain `Simple`/`PreserveAspect` Image with **no masking at all**. Confirmed via pixel-read that `Skin_IconFramePlaceholder.png` genuinely is a circular alpha cutout (corners alpha=0, center alpha=1, not a solid swatch), meaning the ONLY thing making the current placeholder icon look circular was the placeholder texture's own pre-baked alpha shape — any real business icon art assigned to that same Image, unless an artist happened to pre-crop it into an identical circle with matching transparent corners, would have rendered as an uncropped square/rectangle, corners and all.

**Fixed** by adding a real masking layer: `IconFrame` → `IconArtMask` (fixed 92×92, center-anchored square, holds a `UnityEngine.UI.Mask` with `showMaskGraphic=false` driven by the same alpha-cutout `Skin_IconFramePlaceholder` sprite) → `IconArt` (the new actual art slot, `Simple` type, `PreserveAspect=true`). `BusinessDisplayer.image` rewired to `IconArt`. Verified via a generated 1024×1024 opaque test PNG (distinct fill color + thin contrasting border ring, deliberately NOT pre-cropped — the worst-case realistic scenario) imported as a Sprite and assigned live to one row in the open Editor: `Mask.enabled=true`, mask sprite confirmed non-null with the real alpha cutout, `IconArt.preserveAspect=true` so a non-square future source won't stretch. True pixel-level render confirmation still needs a human eyeballing the Editor Game view (per this project's documented Camera/screenshot capture limitations for Screen Space Overlay Canvas UI) — the fix is structurally verified, not yet human-eyeballed.

### `UI_BusinessRow` `IconFrame` had no minimum-width floor — a longer business name silently squeezed the icon
**Component:** `Assets/InfluencerRise/Prefabs/UI/UI_BusinessRow.prefab` (`IconFrame`, `LeftGroup`) · **Severity: Medium** · **Status: Fixed**

Found empirically during the same rebuild pass, by comparing `IconFrame`'s actual computed rect across all 3 live Yourgram rows after a full top-down layout pass: First Post and Story Feed both rendered `IconFrame` at the intended 100px width, but Engagement Farming (a longer business display name) rendered it at only 78.85px. Root cause: `IconFrame`'s `LayoutElement` had a `preferredWidth` but no `minWidth` floor, so when `CenterColumn`'s nested `VerticalLayoutGroup` reported a larger "preferred width" driven by `NameText`'s longer unwrapped content, `LeftGroup`'s `HorizontalLayoutGroup` shrank `IconFrame` proportionally to make room — exactly the class of regression CLAUDE.md Hard Rule #13 already documents ("a sibling's size change can silently collapse an under-specified panel"), just newly instantiated in this specific prefab. Fixed by giving `IconFrame` an explicit `minWidth=100, minHeight=100` floor. Re-verified across all 3 rows post-fix: `IconFrame` rect is now uniformly 100×104 regardless of business name length.

### `Level.AddXp` compounding level-math corruption (marginal `TargetXp` fed to cumulative `GeometricSequence_Amount`)
**Component:** `Assets/EasyIdleGame/Scripts/_Models/Levels/Level.cs` (vendored) · **Severity: Critical** · **Status: Fixed**

Escalated from the "Known open issue" below after a dedicated, definitive investigation (read-only first, then fixed under an explicit scoped exception). Root cause confirmed precisely: `Level.TargetXp` returns the **marginal** cost of the single next level (`Mathb.GeometricSequence_NthTerm` — the n-th term of the sequence, e.g. 1000 → 2000 → 4000 → ...). `Level.AddXp` compared `currentXp` against this marginal value correctly, but then fed that same `currentXp` into `Mathb.GeometricSequence_Amount`, which always interprets its input as an **absolute cumulative sum measured from level 0** — it has no concept of "starting from the current level." Since `currentXp` only ever holds the *leftover remainder* after a level-up (a marginal, current-level-scoped quantity), re-interpreting it as an absolute cumulative total silently mis-counted levels gained and left a nonzero, wrong leftover behind on every level-up past the first — and because each call's leftover was itself already corrupted, the error compounded multiplicatively across successive level-ups.

**Confirmed empirically, isolated from all other code** (direct `Level.AddXp` calls, bypassing `PlayerStats`/claim-loop/custom multiplier systems), granting exactly one level's worth of `TargetXp` at a time, sequentially, before the fix:

| Step | Granted (exact TargetXp) | Resulting level | Expected | Leftover XP |
|---|---|---|---|---|
| 1 | 1,000 | 1 | 1 | 0 (clean) |
| 2 | 2,000 | 2 | 2 | **1,000** (should be 0) |
| 3 | 4,000 | **4** | 3 | 2,000 |
| 5 | 256,000 | **16** | 5 | 4,000 |
| 10 | (continuing) | **512** | 10 | 0 |

Level 1 was clean (no leftover exists yet to corrupt anything); every level from 2 onward was wrong, and by "level 10" the player was actually at level 512 — under the cleanest possible protocol (exact marginal grants, one level at a time), not a crafted edge case. This directly corrupts `currentLevel`, which gates all 15 Businesses, all 3 platform unlocks, and every CloutCoin level-up reward via the Level Progression schedule (see EconomySchema.md) — hence Critical, not Medium as originally flagged.

**Fix:** confined entirely to `Level.AddXp` (no changes to `Mathb.cs` — its functions are correct in isolation and used correctly elsewhere in the codebase; the bug was in how `Level.AddXp` was feeding them, not in the functions themselves). Re-baselines the leftover into the same absolute-from-zero frame the geometric math expects before calling it:

```csharp
// Before:
int addedLevels = (int)Mathb.GeometricSequence_Amount(startXp, incrementMultiplier, currentXp).ToDouble();
currentXp = currentXp - Mathb.GeometricSequence_Sum(startXp, incrementMultiplier, addedLevels);

// After:
BigNumber cumulativeThroughCurrentLevel = Mathb.GeometricSequence_Sum(startXp, incrementMultiplier, currentLevel);
BigNumber absoluteXp = cumulativeThroughCurrentLevel + currentXp;
int newAbsoluteLevel = (int)Mathb.GeometricSequence_Amount(startXp, incrementMultiplier, absoluteXp).ToDouble();
int addedLevels = newAbsoluteLevel - currentLevel;
currentXp = absoluteXp - Mathb.GeometricSequence_Sum(startXp, incrementMultiplier, newAbsoluteLevel);
```

**Re-verified after the fix, all via Unity MCP RunCommand:**
- All 14 of this project's actual milestone levels (3, 7, 12, 14, 18, 24, 28, 34, 42, 55, 65, 80, 110, 150) — granted exact cumulative XP for each from a fresh level-0 reset — landed on the exact expected level with exactly 0 leftover XP, 14/14.
- Sequential levels 1 through 10, one level's worth of marginal XP at a time cumulatively — 10/10 clean, matching the correct doubling costs (1000, 2000, 4000, ... 512000) with 0 drift at any step.
- Interaction with the (separately, previously fixed) `PlayerStats.AddXp` claim-loop bug: a single large XP burst landing exactly on level 55 via the real `PlayerStats.AddXp` path (Growth Hacker's XP top-up temporarily disabled to isolate the two fixes cleanly) resulted in `currentLevel=55` exact, `unclaimedLevelups=0` exact, and CloutCoin level-up rewards totaling 10,150 (7,700 repeating + 2,450 across all 10 milestones in range) — correct to within a `~1.8e-12` floating-point rounding artifact from summing 55 separate `BigNumber` additions (the same class of expected noise as `0.1+0.2≠0.3` in IEEE754 doubles, not a defect). The two fixes coexist correctly.

**Risk:** vendored-file edit — could be silently overwritten by a future EasyIdleGame package update, same as the claim-loop fix. If levels behave oddly after ever updating the package (players not reaching expected levels from their XP, or reaching implausibly high levels from small grants), check this fix first.

### Rare/Legendary Manager passiveBoosts structurally unreachable
**Component:** `Assets/EasyIdleGame/Scripts/Managers/ManagersManager.cs` + `Assets/EasyIdleGame/Scripts/Upgrades/UpgradeEffectResolver.cs` (vendored, root cause) · **Severity: High** · **Status: Fixed/Redesigned (9 of 9 non-Common managers) — all resolved, 0 vendored patches used for any of them**

Found during a full-project manager audit. `ManagersManager.GetPassiveMultiplierOfType`/`GetActiveMultiplierOfType` — the only native code that ever reads a Manager's `passiveBoosts` for business production — only ever look up the one Manager assigned as a *specific Business's own dedicated* `.manager` field. `UpgradeEffectResolver.GetMatchingBlocks` (the generic resolver used for purchaseCost/dropWeight/dropChance/productionOutputScaling) never iterates `ManagersManager` at all. Net effect: any Manager using `targetBusinessGroups` (platform-wide) or an empty/global filter — i.e. all 6 Rare and 3 Legendary managers, by design, per EconomySchema.md — was structurally unreachable. Confirmed empirically in Play Mode (not just code-read): owning 5 levels of a Rare manager left the target business's actual computed production multiplier exactly unchanged. The manager `.asset` files themselves were confirmed correctly configured (e.g. `TheAlgorithmWhisperer.asset` has `value:1.4`, `targetBusinessGroups` correctly pointing at `YourgramAllBusinesses`'s GUID) — this was purely an engine reachability gap, not a content/wiring mistake.

**Fixed via custom code, no vendored edits, for 5 of 9:**
- Production/global-production effects (The Algorithm Whisperer, The Clout Chaser, The Prestige Press, The Main Character) — `Assets/InfluencerRise/Scripts/Managers/RareLegendaryManagerProductionModifier.cs`, a `ProductionModifierAsset` registered on `GameManagers`' `ProductionModifierManager`. Plugs into Easy Idle Game's own already-wired production-output-modifier extension point (`ProductionOutputCalculator` → `ProductionModifierRegistry.ApplyOutputModifiers`). Generic — reads each owned manager's own `targetBusinessGroups` filter directly, so any future Rare/Legendary manager with a production-type passiveBoost is covered automatically, no per-manager entry needed. Empirically verified via a register/unregister toggle around the same production calculation (e.g. HotTake: 8.96K WITH vs 250 WITHOUT The Prestige Press + The Main Character both owned).
- Player XP (The Growth Hacker) — `Assets/InfluencerRise/Scripts/Managers/RareLegendaryManagerEffects.cs`, listens to `PlayerStats.OnXpAdded` and tops up the owed bonus via a second, reentrancy-guarded `AddXp` call. Empirically verified via component-enabled/disabled toggle (50 XP → 375 credited enabled, exactly 50 disabled).

**Redesigned rather than bridged, 3 of 9 — the original mechanism was confirmed to have no discrete event to intercept at all (unlike XP-granting, which does; The Nepo Baby additionally had a second, independent blocker — see its entry below), so bridging any of them as originally specified would require either a live-read vendored patch to a core, constantly-read calculation path, or bypassing a documented completion contract. Deliberately avoided given the severity of the `Level.AddXp` fix above (a vendored patch to core math already caused one Critical bug this project) — redesigned to a different, still level-scaled mechanism instead, implemented via `RareLegendaryManagerEffects.cs`:**
- The Budget Bestie — was -15%/lvl global purchaseCost (confirmed non-functional: cost is read live/repeatedly from `Business.GetBusinessMultiplierOfType` wherever checked or displayed, no discrete grant event). Native `levelRewards`/`eachLevelReward` (`PlayerStats.levelupData`) investigated first as a zero-code alternative and confirmed NOT viable — `LevelRewardHolder` has no ownership/condition field, so a level-up reward can't be gated on owning a Manager natively. Redesigned as a Cash burst granted on every player level-up while owned (responsibility 2, hooks `PlayerStats.OnLevelUpEvent`, grants via `CurrencyManager.AddCurrencyAmount`), scaled by `newPlayerLevel × managerLevel × 10` (placeholder constant, not balance-tested). `passiveBoosts` cleared to `[]`. Verified via MCP with a controlled multi-level burst — Cash gained matched the formula's sum over every level actually crossed, exactly.
- The Algorithm Itself — was +60%/lvl global speed (confirmed non-functional: production timing is read live/repeatedly from `BusinessHolder`'s own timer code, same "no discrete hook" problem as purchaseCost). `Manager.cs` checked first for any native "periodic grant while owned" mechanism — none exists; `activeDuration`/`cooldownDuration`/`activeUpgrade` is a player-*activated* temporary system (`TryActivateManager`), not passive/automatic. Redesigned as periodically re-activating the existing, already-working `AlgorithmPushBoost` (2× speed, 10 min) via a timer in `Update()` (responsibility 3) for as long as owned — `BoostsManager.RemoveActiveBoost` then `SetActiveBoost` (confirmed via full read of `BoostsManager.cs` that `ForceBuyItemsForFree` only adds inventory and does NOT activate anything; these are the real activation methods). **Behavior change, flagged deliberately**: the effect no longer scales with the manager's own level the way "+60%/lvl" implied — owning it now means "AlgorithmPushBoost is always active," not "always active AND stronger at higher levels." `passiveBoosts` cleared to `[]`. The underlying action was verified correct via MCP (manual `RemoveActiveBoost`+`SetActiveBoost` call produces the expected active-boost state); the real-time 60s timer firing on its own could NOT be verified through the MCP bridge — every `Unity_RunCommand` call compiles code, which triggers a domain reload that resets Play Mode's `Time.time`/frame count, so a real-time wait-and-check approach is fundamentally unreliable here (this is the same class of limitation already documented in CLAUDE.md's "Automated Play Mode timing verification does not work through the Unity MCP bridge"). Needs direct Editor observation by a human: enter Play Mode with The Algorithm Itself owned, wait ~60+ seconds, confirm `AlgorithmPushBoost` shows active with a fresh ~600s timer.
- The Nepo Baby — was 8 individually-targeted dropWeight passiveBoosts entries (one per other Rare/Legendary manager, +30%/lvl each), confirmed non-functional for two independent reasons: dropWeight resolves synchronously inside `ShopItemHolder.ClaimRewards` with no pre-roll event to intercept, AND `UpgradeEffectResolver.GetMatchingBlocks` — the code `ModifyDropWeight` itself relies on — never iterates `ManagersManager`'s owned managers at all, so it structurally could never see a Manager-sourced dropWeight passiveBoost regardless of the first problem. A non-vendored in-memory `DropTable`/`Output` clone-and-reweight fix was investigated and found technically feasible ONLY in isolation (both are plain `[Serializable]` C# classes, trivially cloneable) — actually making a reweighted clone's odds govern a real purchase would require bypassing `ShopManager.CompleteExternalPurchase`'s hardcoded `ClaimRewards()` call (the single documented completion path per CLAUDE.md Hard Rule #5, used by all 4 Capsule purchase types), which is not a small pre-roll patch but closer to a parallel purchase-completion path reimplementing that method's full contract. Rejected as too risky/large in scope, same reasoning as avoiding a live-read vendored patch for Budget Bestie/Algorithm Itself. Redesigned instead to amplify an existing, already-working mechanism the same way those two were: grants a bonus CloutCoin amount on top of whatever the native level-up reward already granted, whenever the player levels up while owned (responsibility 4, hooks `PlayerStats.OnLevelUpEvent`, grants via `CurrencyManager.AddCurrencyAmount`) — "born lucky" flavor via more currency to spend on Capsules, rather than directly manipulating pull odds. Formula: `managerLevel × 5` (placeholder `NepoBabyCloutCoinPerLevelMultiplier`, not balance-tested) — a flat per-level-up amount, deliberately not also scaled by player level the way Budget Bestie's two-factor formula is, matching this task's explicit formula rather than silently reusing Budget Bestie's shape. `passiveBoosts` cleared to `[]`. Verified via MCP with a controlled multi-level burst — both the native CloutCoin level-up reward and the Nepo Baby bonus landed exactly matching their respective formulas.

The Wellness Guru was never affected by any of this — it already bypasses the whole broken pipeline via `BurnoutController.cs`'s direct-by-name holder lookup (built before this gap was even found, for an unrelated reason — CustomStats aren't a valid passiveBoosts target at all).

**Risk:** none of the fixes or redesigns touch vendored code, so no package-update risk there. All 9 non-Common managers (6 Rare + 3 Legendary) are now resolved without a single vendored patch — 5 bridged to their originally-specified mechanism via custom code (Algorithm Whisperer, Clout Chaser, Prestige Press, Main Character, Growth Hacker), 3 (Budget Bestie, Algorithm Itself, Nepo Baby) deliberately redesigned to a different, still level-scaled mechanism rather than requiring a live-read vendored patch to a core, constantly-read calculation path, and 1 (Wellness Guru) never affected in the first place. The proper long-term fix (a vendored patch making `ManagersManager`'s lookups and/or `UpgradeEffectResolver.GetMatchingBlocks` iterate all owned managers, matching the same precedent as the `AddXp` fix below) remains available if a future manager's intended mechanism can't be reached any other way, but nothing currently needs it.

### Four confirmed-false "✅ built"/"locked" documentation claims (from commit c89daa7's doc rewrite), fixed for real
**Component:** `PrestigeManager` (scene component), `docs/EconomySchema.md` (3 missing Upgrade assets), `Assets/InfluencerRise/Resources/Upgrades/BetterRingLight.asset` (misdiagnosed, not actually broken) · **Severity: High (2 items), Medium (1 item), N/A — false alarm (1 item)** · **Status: Fixed / Corrected**

Found during a systematic re-verification pass of every "✅ built"/"locked" claim introduced by commit `c89daa7` (a large documentation rewrite that, per prior git-history investigation, asserted several things as done that were never actually implemented). Four items:

1. **`PrestigeManager.resetLevel`** — docs claimed `false` ("Player Level persists — LOCKED decision"), live scene value was actually `true` since the very first commit that configured Prestige (`44b3cfd`), confirmed via git history to have never been `false` at any point — a reporting error, not a regression. **Fixed**: set `resetLevel: 0` (false) on the live `PrestigeManager` component via Edit Mode, scene saved, verified via fresh disk read (`Assets/InfluencerRise/Scenes/Main.unity:9327`).

2. **The 3 "Aura-purchased Permanent Upgrades" (`ClaimYourAlgorithm`, `LuckyBreakInvestment`, `PassiveIncomeGrindset`)** — documented as "✅ 3 built (locked selection, no more planned)" but confirmed via exhaustive git history search to have never existed as asset files at any commit. **Built for real this time**, all under `Assets/InfluencerRise/Resources/Upgrades/`, all native (no custom bridging needed for any of them):
   - `ClaimYourAlgorithm` (10 Aura base, ×1.5/purchase cost scaling, global production ×1.25 placeholder, empty filters — non-contextual, reaches the live business production multiplier the same way `BulkContentBatching`/`FasterEditingSoftware` already do). Verified via MCP: real production output for FirstPost went 80,000 → 100,000 (×1.25 exact) after granting.
   - `LuckyBreakInvestment` (15 Aura base, ×1.5/purchase, dropWeight ×1.3 placeholder, 9 individually-targeted `UpgradeBlock` entries — one per Rare/Legendary manager, `targetManager` filter, same "manual maintenance required" pattern already accepted for the old Nepo Baby design). **Investigated native-vs-bridged feasibility before building, per explicit instruction not to assume**: unlike a Manager's own `passiveBoosts` (structurally invisible to `UpgradeEffectResolver.GetMatchingBlocks` — the root cause of the original Nepo Baby bug), an *Upgrade's* contextual dropWeight blocks ARE correctly iterated by `GetMatchingBlocks`, and `UpgradeEffectContext.WithOutput()` correctly populates `TargetManager = output.managerOutput` per-output during a real Capsule roll — confirmed via full code read, then empirically verified live: `TheMainCharacter`'s real drop weight in `CloutCoinCapsuleHigh` went from 4 → 5.2 (×1.3 exact) after granting, while `RonnyRingo` (Common tier, untargeted) stayed at exactly 1 (correctly unaffected) — fully native, no bridging required.
   - `PassiveIncomeGrindset` (12 Aura base, ×1.5/purchase, production ×1.25 placeholder, `runtimeFilter=Offline`). Verified via MCP: live production ratio stayed exactly 1.0 (unaffected) while `ProfitCalculator.CalculateProfit`'s 300-second offline simulation went ×1.25 exact after granting — confirms the offline-only filter correctly scopes the effect, fully native.
   - All 3 added to `PrestigeManager.excludedUpgrades` (verified via fresh disk read: `Main.unity:9330-9333`); `exludedCurrencies` (Aura, CloutCoin) was already correctly configured and was not disturbed (verified still present: `Main.unity:9344-9346`).

3. **`BetterRingLight` — corrected a self-inflicted misdiagnosis from the prior math audit, not a real bug.** The prior audit concluded BetterRingLight's contextual filter block was "structurally excluded from live production" — that conclusion was wrong, caused by testing the wrong code path. `Business.GetBusinessMultiplierOfType()`/`BusinessHolder.GetProductionMultiplier()` (a "current multiplier" **display** query) genuinely does exclude all `IsContextual` blocks via `IMultiplierProvider.GetMultiplierOfType_NoGroup`, which is what the prior audit measured — but **real production output** (`GetActualProductionForAmount`/`ProductionOutputCalculator.CalculateRolledProduction`, the method that actually computes granted currency, for both live and offline production) routes through a *different*, contextual resolution path (`RewardCalculator.ApplyOutputMultipliers` → `UpgradeEffectResolver.GetMatchingBlocks`) that correctly honors `BetterRingLight`'s `sourceBusiness=FirstPost` filter, because `ProductionOutputCalculator.CreateModifierContext` populates `context.SourceBusiness` (not `TargetBusiness`) for production events — exactly the field `BetterRingLight`'s filter was already using. Re-verified live: FirstPost's real production output went 64,000 → 80,000 (×1.25 exact) with `BetterRingLight` owned. **No fix applied — the asset was never broken.** Left as-is; no group retarget performed (would have been unnecessary rework on already-correct content). This is itself worth internalizing: `GetProductionMultiplier()` is a display-only aggregate that silently omits contextual bonuses — a real, but much lower-severity, UX-display gap (any UI reading that method as a "current multiplier" readout would under-report bonuses from contextual Upgrades), separate from whether the bonus is actually granted.

4. **Burnout order-of-operations near the 0–100 clamp** — the prior math audit could not test this synchronously via MCP (production-round completion is real-time-gated). A clean, deterministic test sequence was run instead (Wellness Guru granted, Spa Day purchased, one production round force-completed via the real `BusinessHolder.OnProductionRoundFinished` method, same direct-invoke pattern already established for tier-scaling tests): Burnout 50 → 50 (Wellness Guru granted, correctly no-op) → 10 (Spa Day, −40, exact) → 10.5 (FirstPost tier-1 production rise, +0.5, exact). This specific run didn't cross the 0/100 boundary, so it doesn't settle the original order-sensitivity question by itself — `ApplyBurnoutDelta`'s clamp-per-call behavior (confirmed via code read) means order provably *would* matter for a sequence that does cross a boundary; that specific numeric confirmation still needs either a boundary-crossing MCP sequence or direct Editor observation.

**Risk:** none of these fixes touch vendored code. Given this is the *second* round of confirmed-false documentation claims traced back to commit `c89daa7`, any remaining unverified claim from that commit should still be treated with suspicion until independently re-checked against live project state — this entry and the systematic audit that preceded it are not necessarily exhaustive.

### AddXp claim-loop drops levels on large XP bursts
**Component:** `Assets/EasyIdleGame/Scripts/PlayerStats/PlayerStats.cs` (vendored package code) · **Severity: High** · **Status: Fixed**

`AddXp`'s level-claim loop re-read `unclaimedLevelups` every iteration while `LevelUp()` simultaneously decremented that same field — loop bound and counter crossed mid-loop, silently dropping the back half of any multi-level XP burst (both level advancement and level-up rewards). Confirmed via repro: 14 levels of XP only advanced the player to level 7, `unclaimedLevelups` stuck at 7. Fixed by caching the target claim count before the loop starts. **Risk: this is a vendored-file edit — could be silently overwritten by a future EasyIdleGame package update.** If levels/rewards start under-firing again after ever updating the package, check this fix first.

### Blank `Id` field on stale Burnout CustomStat holder
**Component:** `PlayerStats.customStats` (scene data) · **Severity: High** · **Status: Fixed**
A leftover test-era Burnout holder with a blank `Id` field sat invisibly correct in the Editor but threw a blocking `EditorUtility.DisplayDialog` the moment it round-tripped through JSON save/load — would have hit any real player on their first app reopen after any session that had touched Burnout ad-hoc. Cleared the stale holder; added `SaveLoadDiagnostics.SuppressLoadErrorDialog` to test scripts going forward.

### Pause-triggered save race condition on startup
**Component:** `AutosaveTrigger.cs` / `GameBootstrap.cs` · **Severity: High** · **Status: Fixed**
`OnApplicationPause(true)` fires automatically and immediately in MCP-driven Play Mode (Unity treats the automation-driven window as backgrounded). This wrote a near-empty save file before `GameBootstrap`'s starting-currency grant ran, making the grant look already-applied on the next load — permanently losing starting Followers. Fixed with `GameBootstrap.HasCompletedInitialLoad`, gating the autosave flush on it.

### Buy Amount bar covered by mis-anchored sibling panel
**Component:** `BusinessListPanel` (UI) · **Severity: Medium** · **Status: Fixed**
`BusinessListPanel`'s top edge was anchored to the same world-Y as `BuyAmountBar`'s top edge instead of its bottom edge, so the list panel rendered directly over and fully covered the buy-amount bar. Looked like a squashed/collapsed layout; was actually a full-coverage overlap. Fixed by correcting `BusinessListPanel`'s offset.

### "Buy 0x" default label/behavior
**Component:** `BusinessDisplayer.cs` (vendor Editor-only default draw) + `UI_BusinessRow.prefab` static text · **Severity: Low (cosmetic)** · **Status: Fixed**
Two separate hardcoded "0x" string literals (an Editor-only placeholder default-draw method, and a static default value baked into the prefab's Text component) — not an actual purchase-logic bug, `buyAmountId` correctly defaulted to index 0 (=1x) the whole time.

### Player leveling structurally deadlocked at Level 0 (untouched engine XP defaults + purchase-only XP source + hard-typed `int` curve field)
**Component:** `Assets/EasyIdleGame/Scripts/PlayerStats/PlayerStats.cs` (vendored, root cause) + `Assets/InfluencerRise/Scripts/PlayerProgression/ProductionXpGrant.cs` (new) + `Assets/InfluencerRise/Scripts/Tools/EconomySimulator.cs` (tuning harness) · **Severity: Critical** · **Status: Fixed**

Found via `EconomySimulator.cs` (see its own Fixed entry below): `PlayerStats.firstLevelXp=1000`/`levelIncrementMultiplier=2` were the untouched EasyIdleGame package defaults, never configured for this project's 150-level/15-milestone schedule (EconomySchema.md's Level Progression table). Player XP is granted **only** on business purchase (`Business.xpsAddPerPurchaseToPlayer`, +1/unit, confirmed via full-codebase investigation — production-round completion granted zero natively, `Output.OnProductionRoundFinished_Apply` has exactly 4 grant fields and none is XP). At 1 XP/purchase against a 1000-XP Level 1 requirement, reaching Level 1 alone needed 1,000 purchases — whose cost was already astronomical — so the game could never progress past Level 0 under real play, gating all 15 Businesses, all 3 platform unlocks, and every CloutCoin level-up reward.

**Two-part fix:**

1. **New XP source** — `ProductionXpGrant.cs`, hooking `BusinessesManager.OnProductionRoundFinishedEvent` (same event/pattern as `BurnoutController.cs` Responsibility 5), grants `tier × 0.5` Player XP per completed production round, tier-scaled 1–5 via the same `BusinessTierByName` lookup pattern. Closes a real design gap independent of any numeric tuning: without it, AFK/idle play (which never manually purchases) had no path to Player XP at all. Mirrored inside `EconomySimulator.cs` for tuning purposes (`ProductionXpPerTierPerRound`, must be kept in sync).

2. **Curve retuning, blocked by a hard-typed `int` field** — iterative tuning via `EconomySimulator.cs` (see its own entry for the full methodology) confirmed no *integer* `levelIncrementMultiplier` value works: `q≥2` (any integer ≥2) hits an exponential wall around Level 15 (confirmed: `startXp=1000,q=2` — the untouched defaults plus the new XP source — stalls at Level 15, ~209.8h/8.7 days, income permanently outpaced by the doubling-per-level requirement); `q=1` (flattest available integer) avoids the wall but produces meaningless multi-million-level inflation within 30 simulated days (per-level cost never grows while production XP keeps trickling in forever — confirmed 17.2M+ levels at `startXp=10,q=1`). Only a fractional `q` (~1.12–1.15, confirmed via simulation to reach Level 3 in minutes and clear the entire Yourgram business chain including its Level-24 capstone with no wall and no blowup) gives sane pacing — but `PlayerStats.levelIncrementMultiplier` was declared `int`, making every fractional candidate physically unwritable to the live component (a fractional value silently truncates to `1` on write, which is what originally surfaced this — not a `BigNumber` display-rounding trap this time, a real type truncation). The runtime `Level` class this value ultimately feeds already stores it as `float` (`Level.incrementMultiplier`, and `Level`'s own constructor already takes `float`) — so the `int` restriction on `PlayerStats`'s exposed field was a pure, unnecessary type mismatch, not a genuine engine constraint. **Fixed via a scoped vendored-code exception** (2nd `PlayerStats.cs` patch this project, explicit fresh authorization obtained — see CLAUDE.md Watch-list): changed `levelIncrementMultiplier` from `int` to `float`. Consumer audit confirmed safe before changing: the only runtime reader is `PlayerStats.ResetLevel()`, which already widens the value into `Level`'s float constructor parameter; the only other references are two `PlayModeTests` files assigning integer literals (safe under widening) and Demo scenes (out of scope, untouched).

**Final chosen values: `firstLevelXp=400`, `levelIncrementMultiplier=1.13`** — applied to the live `PlayerStats` component on `GameManagers`, verified via fresh disk read of `Main.unity`. Chosen from a 4-iteration tuning pass (full log below) to land Level 3 at ~4 simulated minutes (comfortably under the ~30-minute "first session" target from EconomySchema.md's original pacing intent, without being instant) while keeping `q` gentle enough to fully clear the Yourgram business chain.

**Iteration log (ACTIVE / AFK profiles, EconomySimulator.cs, 30-day cap unless noted):**

| # | startXp | q | ACTIVE Level 3 | ACTIVE result | AFK Level 3 | AFK result |
|---|---|---|---|---|---|---|
| 1 | 1000 | 2 (untouched defaults) | 15.5 min | Stalls at Level 15, ~209.8h (8.7d) — exponential wall | 3916.5 min (~65.3h) | Stalls hard by hour 7, recovers to Level 17 by day 30 |
| 2 | 100 | 1.15 | 1.5 min | Stalls at Level 80, ~246.7h (10.3d) — all 5 Yourgram businesses fully purchased (incl. Level-24 capstone VerifiedStatus), reaches Level 89 by day 30 via production-XP trickle | ~65h (automation-ETA-gated) | Reaches Level 88 by day 30 |
| 3 | 50 | 1.12 (60-day cap, testing structural-vs-time-limited) | 1.0 min | Stalls at Level 103, **identical** `simSeconds=888010` and owned-business breakdown to Iteration 2 — confirms the wall is structural (Cash-bootstrap, see Known Open Issue below), not time- or curve-limited | ~65h | Reaches Level 120 by day 60 |
| 4 (final) | 400 | 1.13 | **4.2 min** | Stalls (Cash-bootstrap only) at Level 79, ~246.7h; reaches **Level 89 by day 30** with no XP-side wall | 3901.7 min (~65h, governed by Manager-acquisition ETA, not the XP curve) | Reaches Level 88 by day 30 |

All 4 iterations' AFK Level-3 time (~65h) is governed by the Common-Manager expected-acquisition ETA (closed-form, 16.2 expected FreeCapsule pulls × 4h cooldown ≈ 64.8h) — confirmed XP-curve-independent across every candidate tested, since AFK makes almost no purchases before automation kicks in.

**Note on this task's own simulator bug, self-caught during tuning:** `EconomySimulator.CumulativeXpForLevel`'s closed-form `(q^level-1)/(q-1)` divides by zero at `q=1`, producing `NaN`; since `NaN <= x` is always false in IEEE754, this silently froze all 3 initial `q=1` test candidates at `Level=0` forever regardless of `startXp` (looked like a tuning result, was actually a simulator defect). Fixed by mirroring the real engine's own `Mathb.GeometricSequence_Sum`'s `if (q==1) return a*n;` special case, which the simulator had not replicated.

**Note on a second simulator-comparison pitfall, self-caught during the Manager Impact Comparison re-run:** comparing `FinalFollowers` currency *balance* between two runs at the same wall-clock cutoff is misleading — `TryReinvest`'s greedy buy-cheapest-affordable loop can consume a large fraction of the balance in a single purchase once an expensive unit becomes affordable near the exponential (~1.15) business cost curve, so two runs sampled at the same cutoff can land at different points in that sawtooth pattern (the Algorithm Whisperer run initially appeared to end with *89.9% fewer* Followers than the non-Whisperer run, despite owning strictly more of every business). Added a monotonic `SimResult.FinalOwnedCounts` field for honest comparison instead — see the Manager Impact Comparison re-run entry below.

**Separate, unresolved finding surfaced during tuning (see Known Open Issue below):** no XP curve, however tuned, can make Level 150 reachable — confirmed structural (Iteration 3's 60-day, gentler-curve test hit the identical wall at the identical `simSeconds`) and caused by a completely separate Cash-bootstrap gap in the QuickTok/Broadcast business chain, not by anything XP-related. Flagged rather than silently worked around, per CLAUDE.md Hard Rule #14 (falls under the "no Business cost curve changes" hard boundary of the task that found it).

**Risk:** two vendored-file edits now live in `PlayerStats.cs` (this one + the pre-existing `AddXp` claim-loop fix) — both could be silently overwritten by a future EasyIdleGame package update. See consolidated CLAUDE.md Watch-list entry.

### `EconomySimulator.cs` (Phase 6 balancing harness)
**Component:** `Assets/InfluencerRise/Scripts/Tools/EconomySimulator.cs` (new, custom) · **Severity: N/A — new tooling** · **Status: Built, extended, in active use**
Standalone economy-pacing simulator, reads real configured asset data via `Resources.Load` and reimplements this project's own verified formulas (geometric business cost curve, Manager passive-effect formula, native purchase-XP grant) operating entirely on local simulation state — never touches any live singleton, so it cannot mutate real save state. Runs `ACTIVE` (continuous manual reinvestment) and `AFK`/`Idle` (hourly-checkpoint reinvestment, automation-gated production) profiles against the full 150-level schedule; supports a forced-grant flag for Manager Impact Comparisons. Extended this session to model the new `ProductionXpGrant.cs` XP source and accept tunable `firstLevelXp`/`q` parameters (previously hardcoded to the then-live 1000/2 defaults) — see the XP deadlock entry above for how this harness was used to find and verify the fix, including two self-caught defects in the simulator itself (a `q=1` division-by-zero/NaN freeze, and a misleading currency-balance comparison metric, both documented in that entry). Deliberately excludes Prestige/Rebrand, Boosts, and real per-pull Manager RNG (modeled instead as closed-form expected-value acquisition time) — a conservative/floor pacing estimate, not an inflated one; see the file's own header for full scope notes.

### Manager Impact Comparison re-run (Algorithm Whisperer, fixed XP config)
**Component:** `Assets/InfluencerRise/Scripts/Tools/EconomySimulator.cs` · **Severity: N/A — balancing data** · **Status: Complete**
Re-run against the fixed `firstLevelXp=400,q=1.13` config (the original comparison, run against the pre-fix deadlocked config, was not meaningful — dwarfed by the Level-0 wall). ACTIVE profile, 30-day cap, comparing Algorithm Whisperer granted-at-Level-1 vs not granted. `FinalFollowers` balance is not a valid comparison metric here (see the sawtooth-pattern note in the XP deadlock entry above) — using total units owned instead (monotonic): **without = 481 units** (FirstPost 117/SelfieSession 109/StoryFeed 98/EngagementFarming 84/VerifiedStatus 73), **with = 495 units** (120/112/101/87/75) — **+14 units, +2.9%**. Milestone-level timestamps are also consistently faster with the Whisperer (e.g. Level 3: 4.0min vs 4.2min; Level 80: 14,495min vs 14,911min — roughly 3–4% faster throughout). The modest size of the effect reflects that overall progress is bottlenecked by the separate Cash-bootstrap wall (see Known Open Issue below) once the Yourgram chain maxes out — a platform-scoped (Yourgram-only) production multiplier has limited room to help once that structural ceiling dominates.

### `BigNumber.ToString()` rounds small fractional values for display, not just large ones
**Component:** `Assets/EasyIdleGame/Scripts/BigNumber/BigNumber.cs` (vendor) · **Severity: N/A — not a bug, testing gotcha** · **Status: Resolved (no code change, documentation only)**
Found while testing `BurnoutController.cs` Responsibility 5's tier-scaled Burnout gain (placeholder values 0.5/2.5, not whole numbers). Logging a `BigNumber` via its default `{0}`/`ToString()` formatting - built for compact big-currency display like "1.5K" or "2.3M" - rounds small fractional values to the nearest whole number too: a Burnout stat that had genuinely and precisely gone from 0 to 0.5 displayed as "1" in test logs, momentarily looking like the gain had been rounded or doubled. Confirmed via `.Mantissa`/`.Exponent`/`.ToDouble()` read directly: the underlying value was exactly 0.5 the whole time, full precision preserved through `PlayerStats.IncrementStat`. Not a bug in any game logic - flagged here purely so a future session testing a small/fractional CustomStat or currency value doesn't lose time re-diagnosing the same false alarm. When precision matters for a test assertion, read `.ToDouble()` (or `.Mantissa`/`.Exponent`) directly instead of trusting the logged/displayed string.

---

## Known open issues (not yet fixed)

### Offline/idle progress grants zero Player XP — only active play advances level
**Component:** `Assets/EasyIdleGame/Scripts/ProfitCalculation/ProfitCalculator.cs` (vendored, native) · **Severity: High** · **Status: Open — logged, not fixed, needs a scoped decision**

Found while diagnosing why a long-lived dev/test save (240M+ Followers, 132+ First Post purchases) was still stuck at Player Level 0. Confirmed via full read of `ProfitCalculator.cs`: `CalculateProfit`/`ApplyProfit` (the closed-form "welcome back" offline-profit calculation that runs on load, per `SaveAndLoad.cs`'s own doc comment "applies offline profit after loading") never calls `PlayerStats.AddXp` and never fires `BusinessesManager.OnProductionRoundFinishedEvent` — it computes a bulk currency payout for elapsed offline time directly, bypassing the per-round event pipeline entirely. `ProductionXpGrant.cs` (this project's custom XP source for idle/automated production, see its own Fixed entry above) only listens to that event, so it structurally cannot fire for offline-simulated rounds.

**Practical effect:** a player who plays this idle game the normal way — closes the app, comes back later to a big offline-earnings payout — receives currency and (if `Business.xpsAddPerPurchaseToPlayer`-triggering auto-reinvestment happens offline) possibly more owned businesses, but **zero Player XP for any of it**. Only XP earned during actual live/foreground play (purchases via `BusinessHolder.cs:380`, or live production rounds via `ProductionXpGrant.cs`) counts. Over time this permanently starves leveling relative to currency/business growth for any player who relies on idle/offline play — which is the core promise of an idle-tycoon game — compounding the already-known-slow leveling curve documented in the "Player leveling structurally deadlocked" fix above.

**Not fixed as part of this task** — flagged for a scoped decision later (same category as the `FreeCapsule` cooldown gap and the `ShopItem` cooldown gap already tracked in the Watch-list). Two shapes a fix could reasonably take, for whoever picks this up: (a) grant a lump-sum XP amount alongside the offline currency payout, scaled by elapsed simulated production rounds (mirrors `ProductionXpGrant.cs`'s tier-scaled formula); or (b) have `CalculateProfit`/`ApplyProfit` actually loop through simulated rounds and fire the same event `ProductionXpGrant.cs` listens to, so the two systems can never drift apart again. Needs balance-tuning either way (same iterative `EconomySimulator.cs`-driven methodology as the original leveling-curve fix), not just a mechanical patch.

### QuickTok Cash-bootstrap chain — 3 stranded businesses found and fixed; Broadcast unaffected; full 150-level schedule now blocked only by the (separate, already-tuned) XP curve
**Component:** `TrendChase.asset`, `CollabFarming.asset`, `ReactionContent.asset` (all 3 fixed) · **Severity: Critical** · **Status: Fixed — full economy dependency trace complete, all currency-stranding instances resolved**

**Full dependency trace, all 15 businesses, real `cost`/`locks`/`outputTables` data (not simulator assumptions):**

| Platform | Business | Costs | Produces | Currency earnable at that point? | Verdict |
|---|---|---|---|---|---|
| Yourgram | FirstPost | Followers (1) | Followers | Yes — GameBootstrap seeds 10 Followers | OK |
| Yourgram | SelfieSession | Followers (3) | Followers | Yes — FirstPost already producing | OK |
| Yourgram | StoryFeed | Followers (15) | Followers | Yes | OK |
| Yourgram | EngagementFarming | Followers (100) | Followers | Yes | OK |
| Yourgram | VerifiedStatus | Followers (500) | Followers | Yes | OK |
| QuickTok | TrendChase | ~~Cash (50)~~ → **Followers (300)** | Followers | **No (Cash) before fix** — nothing produces Cash yet | **STRANDED → FIXED** |
| QuickTok | CollabFarming | ~~Cash (120)~~ → **Followers (800)** | Followers | **No (Cash) before fix** — TrendChase only produces Followers | **STRANDED → FIXED** |
| QuickTok | ReactionContent | ~~Cash (300)~~ → **Followers (4000)** | Cash | **No (Cash) before fix** — self-referential: it's the chain's first Cash *producer*, but nothing before it ever produced Cash to seed its own first purchase | **STRANDED → FIXED** |
| QuickTok | AlgorithmBaiting | Cash (700) | Cash | Yes, once ReactionContent is owned and producing | OK (confirmed via re-sim) |
| QuickTok | ViralChallenge | Cash (1500) | Followers | Yes, AlgorithmBaiting/ReactionContent producing Cash | OK (confirmed via re-sim) |
| Broadcast | HotTake | Cash (2000) | Cash | Yes, QuickTok's Cash chain already flowing by Level 34 (needs 1× ReactionContent owned) | OK (confirmed via re-sim) |
| Broadcast | UnboxingHaul | Cash (4000) | Cash | Yes, HotTake producing | OK (confirmed via re-sim) |
| Broadcast | Streaming | Cash (6000) | Cash | Yes, UnboxingHaul producing | OK (confirmed via re-sim) |
| Broadcast | SponsorDeals | Cash (8000) | Cash | Yes, Streaming producing (confirmed reachable given enough time — see below) | OK |
| Broadcast | OwnBrand | Cash (24000) | Cash | Yes, SponsorDeals producing (confirmed reachable given enough time — see below) | OK |

**Every stranded business found and fixed (3 total, all QuickTok, all the same pattern — cost a currency with zero prior source in the reachable chain):**
- `TrendChase`: 50 Cash → **300 Followers** (fixed in a prior pass this project).
- `CollabFarming`: 120 Cash → **800 Followers**. Chosen via `EconomySimulator.cs` candidate testing (500/800/1500 Followers), following the same ~2.7x-per-tier escalation as `TrendChase`→`CollabFarming`, broadly consistent with Yourgram's own tier-to-tier cost jumps (1→3→15→100→500).
- `ReactionContent`: 300 Cash → **4000 Followers**. Candidate-tested (1000/3000/4000/7000/8000 Followers); 4000 continues the escalation (300→800→4000, ~5x) and produced sensible early-QuickTok pacing (first purchase ~1h after arrival, ACTIVE profile).
- `costMultiplier` (1.15) left unchanged on all 3, per hard boundary — only cost currency/amount changed.

**Broadcast chain assessment: NOT independently affected by this pattern (confirmed, not assumed).** Traced `HotTake` through `OwnBrand`: every Broadcast business costs Cash and is unlocked only after a QuickTok Cash-producer (`ReactionContent`, Level 28) is already owned and producing — by the time Broadcast's own gate (Level 34 + 1× `ReactionContent` + 5000-Cash travel fee, itself newly modeled in `EconomySimulator.cs` via `LoadPlatformTravelCosts`/`TryUnlockPlatforms`, previously never simulated) is reachable, real Cash income already exists. Re-simulation with all 3 fixes applied confirms this empirically: `HotTake` (62 owned), `UnboxingHaul` (58), `Streaming` (55) all purchased in growing quantity by day 60, `Cash` balance in the millions. No Broadcast business needed its cost currency changed.

**Level 150 still not reached within a 60-day cap — root cause confirmed to be a *different*, already-known mechanism, not a new instance of this pattern.** Extended-cap testing (60d → 180d → 365d → 730d, ACTIVE profile) shows Level growth is not stalled/walled — Level 110 is reached at day 240, and `SponsorDeals` starts being purchased once it is — but decelerates sharply the further past ~Level 90 it goes (+30 levels in the 125 days after Level 80, vs. only +6 levels across the entire second year). This matches the exponential `levelIncrementMultiplier=1.13` XP curve (fixed in a prior task, explicitly out of scope this round — "no XP values touched") compounding faster than purchase-driven income can keep pace with, once the ~1.15 business cost curve also starts compounding heavily. This is the same category of tension the original XP-tuning task addressed at lower levels, now surfacing again at a higher level threshold once the currency-stranding walls were cleared — a finite, non-circular pacing limit, not a new stranded-currency instance. Reported per this task's own exit clause (item 5) rather than touched, since fixing it would require changing XP values, which this round's hard boundary explicitly forbids.

**Also discovered during this investigation (kept from the prior pass):** `GameBootstrap.cs` grants only Followers (no Cash seed); the one Cash-related Shop item, `CashPackSmall`, has an empty `productId` and empty `inGameCost` — an unconfigured real-money-IAP stub, not a usable free Cash source. `EconomySimulator.cs`'s own currency-reading (`Business.Outputs`, correctly flattening `outputTables`) was independently audited and confirmed correct — no simulator bug was ever the cause of any part of this deadlock.

### `FreeCapsule` had no native cooldown field
**Component:** `ShopItem.cs` (vendor) · **Severity: N/A — workaround shipped** · **Status: Resolved via custom code**
Not a bug, a gap: no native recurring-interval field on ShopItem. Resolved via `FreeCapsuleCooldown.cs` (custom, PlayerPrefs-based). Listed here for traceability, not as an active issue.

---

## Template for new entries

```
### [Short title]
**Component:** [file/system] · **Severity: [Low/Medium/High/Critical]** · **Status: [Open/Fixed/Investigating]**
[What was wrong, how it was found, the fix if any, any residual risk.]
```