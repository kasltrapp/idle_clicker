# Easy Idle Game demos

The full demo catalog — every scene, its package-relative location, and what it demonstrates — is [Demo catalog](../Documentation/md/06-demo-catalog.md) (`EasyIdleGame > Documentation > md > 06-demo-catalog.md`).

Quick start: open `EasyIdleGame > Demos > FeatureDemos > 01_BasicFlow > BasicFlow.unity`, then select the adjacent `ReadMe.asset` in the Inspector for setup notes and the resource prefix.
